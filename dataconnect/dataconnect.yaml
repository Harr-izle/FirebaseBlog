specVersion: "v1beta"
serviceId: "firebase-blog"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-blog-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
